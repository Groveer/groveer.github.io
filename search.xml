<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Arch Linux(MBR and UEFI)安装</title>
      <link href="/2023/01/07/archlinux-install/"/>
      <url>/2023/01/07/archlinux-install/</url>
      
        <content type="html"><![CDATA[<p>确认主板系统是UEFI,这里使用GPT分区格式，关于究竟该使用MBR还是GPT请参考<a href="https://wiki.archlinux.org/title/Partitioning_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E9%80%89%E6%8B%A9_GPT_%E8%BF%98%E6%98%AF_MBR">这里</a>。</p><h2 id="下载Arch-Linux镜像"><a href="#下载Arch-Linux镜像" class="headerlink" title="下载Arch Linux镜像"></a>下载Arch Linux镜像</h2><p><a href="https://www.archlinux.org/download/">下载地址</a></p><p>向下滚动到China下载，网易的节点就很快。</p><h2 id="验证镜像完整性"><a href="#验证镜像完整性" class="headerlink" title="验证镜像完整性"></a>验证镜像完整性</h2><p><code>md5 archlinux-2021.05.01-x86_64.iso</code></p><p>将输出和下载页面提供的md5值对比一下，看看是否一致，不一致则不要继续安装，换个节点重新下载，直到一致为止。</p><h2 id="将镜像写入U盘"><a href="#将镜像写入U盘" class="headerlink" title="将镜像写入U盘"></a>将镜像写入U盘</h2><ul><li><strong>Linux&#x2F;Unix: dd</strong></li></ul><ol><li>用<code>lsblk</code>找到U盘并确保没有挂载，使用<code>umount /dev/sdb1</code>卸载</li><li>使用dd命令：<code>sudo dd bs=4M if=path/to/archlinux-version-x86_64.iso of=/dev/sdx conv=fsync oflag=direct status=progress</code></li></ol><ul><li><strong>MacOS: dd</strong></li></ul><ol><li><code>sudo su - root</code> 输入开机密码，获取root权限。</li><li>查一下iso镜像在哪，pwd得到路径，我的在<font color=green>&#x2F;Users&#x2F;guo&#x2F;Desktop&#x2F;archlinux-2021.05.01-x86_64.iso</font></li><li>查看U盘挂载点：<code>df -h</code>，我的是<font color=green>&#x2F;dev&#x2F;disk2</font></li><li>卸载U盘再装：<code>diskutil unmountDisk /dev/disk2</code></li><li><code>dd if=/Users/guo/Desktop/archlinux-2021.05.01-x86_64.iso of=/dev/disk2 bs=1m</code></li></ol><ul><li><strong>Windows: <a href="https://sourceforge.net/projects/usbwriter/">USBWriter</a></strong></li></ul><h2 id="从U盘启动Arch-live环境"><a href="#从U盘启动Arch-live环境" class="headerlink" title="从U盘启动Arch live环境"></a>从U盘启动Arch live环境</h2><p>在<code>BIOS</code>中设置启动磁盘为刚刚写入镜像的U盘，一般来说开机按<code>F12</code>即可选择启动盘，如果不行，则需<code>F2</code>或<code>DEL</code>进入BIOS修改U盘为优先启动，注意装完系统后将U盘启动改回后面去。<br><br>进入U盘的启动引导程序后，选择第一项即可。</p><h2 id="连接internet"><a href="#连接internet" class="headerlink" title="连接internet"></a>连接internet</h2><ul><li><p>查看连接：<code>ip link</code></p></li><li><p>连接</p><p>  对于有线网络，安装镜像启动的时候，会默认启动dhcpcd，如果没有启动，可以手动启动：<code>dhcpcd</code></br><br>  对于无线网络，官方推荐使用<a href="https://wiki.archlinux.org/title/Iwd_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#iwctl">iwctl</a></p></li></ul><h2 id="磁盘分区"><a href="#磁盘分区" class="headerlink" title="磁盘分区"></a>磁盘分区</h2><ul><li><p>查看磁盘设备<code>lsblk -f</code></p></li><li><p>新建分区表：由于我的本子有两块硬盘，一块固态，一块机械，所以我将固态盘作为系统分区，机械盘作为数据分区，即根、boot、swap分区在固态盘上，home分区在机械盘上。<br><br>  <code>fdisk /dev/sda</code><br></p><ol><li>输入<code>g</code>，新建GPT分区表</li><li>输入<code>w</code>，保存修改，这个操作会抹掉磁盘所有数据，慎重执行！</li><li>对<code>/dev/sdb</code>做同样处理。</li></ol></li><li><p>分区创建<br><br>  1扇区 &#x3D; 512字节<br><br>  <code>fdisk /dev/sda</code><br></p><ol><li>新建 EFI System 分区（非UEFI引导可以省略此步）<br><ol><li>输入<code>n</code></li><li>选择分区区号，直接<code>Enter</code>，使用默认值，fdisk 会自动递增分区号</li><li>分区开始扇区号，直接<code>Enter</code>，使用默认值</li><li>分区结束扇区号，输入<code>+512M</code>（推荐大小）</li><li>输入<code>t</code>修改刚刚创建的分区类型</li><li>选择分区号，直接<code>Enter</code>， 使用默认值，fdisk 会自动选择刚刚新建的分区</li><li>输入<code>1</code>，使用 EFI System 类型</li></ol></li><li>新建 Linux root (x86-64) 分区<ol><li>输入<code>n</code></li><li>选择分区区号，直接<code>Enter</code>，使用默认值，fdisk 会自动递增分区号</li><li>分区开始扇区号，直接<code>Enter</code>，使用默认值</li><li>分区结束扇区号，这里要考虑预留给 swap 分区空间，计算公式：root 分区结束扇区号 &#x3D; 磁盘结束扇区号 - 分配给 swap 分区的空间 (GB) * 1024 * 1024 * 1024 &#x2F; 512，输入后<code>Enter</code></li><li>输入<code>t</code>修改刚刚创建的分区类型</li><li>选择分区号，直接<code>Enter</code>， 使用默认值，fdisk 会自动选择刚刚新建的分区</li><li>输入<code>23</code>，使用 Linux root (x86-64) 类型</li></ol></li><li>新建 Linux swap 分区（使用<a href="https://wiki.archlinux.org/title/Swap_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E4%BA%A4%E6%8D%A2%E6%96%87%E4%BB%B6">交换文件</a>可以省略此步）<ol><li>输入<code>n</code></li><li>选择分区区号，直接<code>Enter</code>，使用默认值，fdisk 会自动递增分区号</li><li>分区开始扇区号，直接 Enter，使用默认值</li><li>分区结束扇区号，比如<code>+8G</code>，这里直接使用默认值，剩下的空间均给 swap 分区</li><li>输入<code>t</code>修改刚刚创建的分区类型</li><li>选择分区号，直接<code>Enter</code>， 使用默认值，fdisk 会自动选择刚刚新建的分区</li><li>输入<code>19</code>，使用 Linux swap 类型</li></ol></li><li>保存新建的分区<ol><li>输入<code>w</code></br></li></ol></li></ol></li><li><p>机械盘进行分区：<code>fdisk /dev/sdb</code><br></p><ol><li>新建 Linux root (x86-64) 分区<ol><li>输入<code>n</code></li><li>选择分区区号，直接<code>Enter</code>，使用默认值，fdisk 会自动递增分区号</li><li>分区开始扇区号，直接<code>Enter</code>，使用默认值</li><li>分区结束扇区号，直接<code>Enter</code>，使用默认值，使用全部空间</li><li>输入<code>t</code>修改刚刚创建的分区类型</li><li>选择分区号，直接<code>Enter</code>， 使用默认值，fdisk 会自动选择刚刚新建的分区</li><li>输入<code>23</code>，使用 Linux root (x86-64) 类型</li></ol></li><li>保存新建的分区<ol><li>输入<code>w</code><br></li></ol></li></ol></li></ul><h2 id="磁盘格式化"><a href="#磁盘格式化" class="headerlink" title="磁盘格式化"></a>磁盘格式化</h2><ul><li>格式化 EFI System 分区：<code>mkfs.fat -F32 /dev/sda1</code><br><br>  如果格式化失败，可能是磁盘设备存在 Device Mapper，一般来说使用了lvm，会存在这种情况<ul><li>显示 dm 状态：<code>dmsetup status</code></li><li>删除 dm：<code>dmsetup remove &lt;dev-id&gt;</code></li></ul></li><li>格式化 Linux root 分区：<code>mkfs.ext4 /dev/sda2</code>、<code>mkfs.ext4 /dev/sdb1</code></li><li>格式化 Linux swap 分区：<code>mkswap /dev/sda3</code> &amp;&amp; <code>swapon /dev/sda3</code></li></ul><h2 id="挂载文件系统"><a href="#挂载文件系统" class="headerlink" title="挂载文件系统"></a>挂载文件系统</h2><p>在这里需要将除 swap 分区外，其他分区都挂上去，注意先后顺序不能错<br></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">mount /dev/sda2 /mnt</span><br><span class="line">mkdir /mnt/boot</span><br><span class="line">mkdir /mnt/home</span><br><span class="line">mount /dev/sda1 /mnt/boot</span><br><span class="line">mount /dev/sdb1 /mnt/home</span><br></pre></td></tr></table></figure><p>交换分区启用:<br></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mkswap /dev/sda3</span><br><span class="line">swapon /dev/sda3</span><br></pre></td></tr></table></figure><h2 id="配置-pacman-mirror"><a href="#配置-pacman-mirror" class="headerlink" title="配置 pacman mirror"></a>配置 pacman mirror</h2><p>一般来说，不需要进行修改，如果发现下载速度很慢，可以修改其中顺序或加入<a href="https://wiki.archlinux.org/title/Mirrors_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E4%B8%AD%E5%9B%BD">中国节点</a>的地址<br></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/pacman.d/mirrorlist`</span><br></pre></td></tr></table></figure><p>或直接使用<code>reflector</code>对源速度进行排序：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">reflector --country China --protocol http --protocol https --latest 5 --save /etc/pacman.d/mirrorlist</span><br></pre></td></tr></table></figure><h2 id="安装-Arch-和-Package-Group"><a href="#安装-Arch-和-Package-Group" class="headerlink" title="安装 Arch 和 Package Group"></a>安装 Arch 和 Package Group</h2><p><code>pacstrap /mnt base base-devel linux linux-firmware</code></p><h2 id="生成-fstab-文件"><a href="#生成-fstab-文件" class="headerlink" title="生成 fstab 文件"></a>生成 fstab 文件</h2><p><code>genfstab -U /mnt &gt;&gt; /mnt/etc/fstab</code></p><h2 id="切换至安装好的-Arch"><a href="#切换至安装好的-Arch" class="headerlink" title="切换至安装好的 Arch"></a>切换至安装好的 Arch</h2><p><code>arch-chroot /mnt</code></p><h2 id="安装必备工具"><a href="#安装必备工具" class="headerlink" title="安装必备工具"></a>安装必备工具</h2><p><code>pacman -S vim networkmanager sudo</code></p><h2 id="本地化"><a href="#本地化" class="headerlink" title="本地化"></a>本地化</h2><ul><li>修改 &#x2F;etc&#x2F;locale.gen，取消注释下面这两行配置：<br>  <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">en_US.UTF-8 UTF-8</span><br><span class="line">zh_CN.UTF-8 UTF-8</span><br></pre></td></tr></table></figure></li><li>生成 locale 信息：<code>locale-gen</code></li><li>创建 &#x2F;etc&#x2F;locale.conf 并写入：<code>LANG=en_US.UTF-8</code></li></ul><h2 id="网络配置"><a href="#网络配置" class="headerlink" title="网络配置"></a>网络配置</h2><ul><li>修改 hostname，创建 &#x2F;etc&#x2F;hostname 并写入：<code>Arch</code></li><li>配置 hosts，编辑 &#x2F;etc&#x2F;hosts<figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1 localhost</span><br><span class="line">::1       localhost</span><br><span class="line">127.0.1.1 Arch.localdomain</span><br></pre></td></tr></table></figure></li></ul><h2 id="修改-root-密码"><a href="#修改-root-密码" class="headerlink" title="修改 root 密码"></a>修改 root 密码</h2><p><code>passwd</code></p><h2 id="安装-Microcode"><a href="#安装-Microcode" class="headerlink" title="安装 Microcode"></a>安装 Microcode</h2><ul><li>AMD CPU：<code>pacman -S amd-ucode</code></li><li>Intel CPU：<code>pacman -S intel-ucode</code></li></ul><h2 id="安装-GRUB"><a href="#安装-GRUB" class="headerlink" title="安装 GRUB"></a>安装 GRUB</h2><p>MBR引导：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pacman -S grub</span><br><span class="line">grub-install --target=i386-pc /dev/sda</span><br><span class="line">grub-mkconfig -o /boot/grub/grub.cfg</span><br></pre></td></tr></table></figure><p>UEFI引导：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pacman -S grub efibootmgr</span><br><span class="line">grub-install --target=x86_64-efi --efi-directory=/boot --bootloader-id=grub</span><br><span class="line">grub-mkconfig -o /boot/grub/grub.cfg</span><br></pre></td></tr></table></figure><h2 id="重新启动"><a href="#重新启动" class="headerlink" title="重新启动"></a>重新启动</h2><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">exit    # 退出 chroot 环境，或按 Ctrl+D</span><br><span class="line">reboot</span><br></pre></td></tr></table></figure><h2 id="重启后的设置"><a href="#重启后的设置" class="headerlink" title="重启后的设置"></a>重启后的设置</h2><p>使用root账户登录后：</p><ul><li>开启时间自动同步<figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">timedatectl set-ntp true</span><br><span class="line">timedatectl set-timezone Asia/Shanghai</span><br></pre></td></tr></table></figure></li><li>新建用户<figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">useradd -m &lt;username&gt;</span><br></pre></td></tr></table></figure></li></ul>]]></content>
      
      
      <categories>
          
          <category> ArchLinux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
