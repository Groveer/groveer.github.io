<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>ArchLinux 系统安装，保姆级教程</title>
      <link href="/2023/01/07/archlinux-install/"/>
      <url>/2023/01/07/archlinux-install/</url>
      
        <content type="html"><![CDATA[<p>ArchLinux 安装不是最难的，但也不是傻瓜式难度安装（有手就行），安装 ArchLinux 不仅需要动动手指，还需要有一台电脑，有一个 U 盘，还必须有可以访问互联网的网络。<br>ArchLinux 的安装并不是很难，只要了解了 Linux 启动流程，就可以理解它的大部分安装步骤。<br>首先需要确认主板系统是 UEFI，这里使用 GPT 分区格式，关于究竟该使用 MBR 还是 GPT 请参考<a href="https://wiki.archlinux.org/title/Partitioning_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E9%80%89%E6%8B%A9_GPT_%E8%BF%98%E6%98%AF_MBR">这里</a>。</p><h2 id="下载Arch-Linux镜像"><a href="#下载Arch-Linux镜像" class="headerlink" title="下载Arch Linux镜像"></a>下载Arch Linux镜像</h2><p><a href="https://www.archlinux.org/download/">下载地址</a></p><p>向下滚动到 China 下载，网易的节点就很快。</p><h2 id="验证镜像完整性"><a href="#验证镜像完整性" class="headerlink" title="验证镜像完整性"></a>验证镜像完整性</h2><div class="tabs" id="tab-id"><ul class="nav-tabs"><li class="tab active"><a class="#tab-id-1">Linux/Unix</a></li><li class="tab"><a class="#tab-id-2">MacOS</a></li><li class="tab"><a class="#tab-id-3">Windows</a></li></ul><div class="tab-content"><div class="tab-pane active" id="tab-id-1"><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">md5sum archlinux-x86_64.iso</span><br></pre></td></tr></table></figure></div><div class="tab-pane" id="tab-id-2"><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">md5 archlinux-x86_64.iso</span><br></pre></td></tr></table></figure></div><div class="tab-pane" id="tab-id-3"><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">certutil -hashfile .\archlinux-x86_64.isop MD5</span><br></pre></td></tr></table></figure></div></div></div><p>将输出和下载页面提供的 md5 值对比一下，看看是否一致，不一致则不要继续安装，换个节点重新下载，直到一致为止。</p><h2 id="将镜像写入U盘"><a href="#将镜像写入U盘" class="headerlink" title="将镜像写入U盘"></a>将镜像写入U盘</h2><div class="tabs" id="tab-id"><ul class="nav-tabs"><li class="tab active"><a class="#tab-id-1">Linux/Unix</a></li><li class="tab"><a class="#tab-id-2">MacOS</a></li><li class="tab"><a class="#tab-id-3">Windows</a></li></ul><div class="tab-content"><div class="tab-pane active" id="tab-id-1"><ol><li><p>确保插上电脑的 U 盘没有被挂载，某些桌面环境会自动挂载 U 盘。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lsblk</span><br></pre></td></tr></table></figure><p>可能会显示以下内容：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">NAME     MAJ:MIN RM   SIZE RO TYPE MOUNTPOINTS</span><br><span class="line">sda      8:0    0 931.5G  0 disk</span><br><span class="line">└─sda1    8:1    0 931.5G  0 part /home</span><br><span class="line">sdb      8:16   1  29.9G  0 disk</span><br><span class="line">└─sdb1    8:17   1  29.9G  0 part /run/media/guo/Arch</span><br><span class="line">nvme0n1    259:0    0 238.5G  0 disk</span><br><span class="line">├─nvme0n1p1 259:1    0   512M  0 part /boot</span><br><span class="line">└─nvme0n1p2 259:2    0   238G  0 part /</span><br></pre></td></tr></table></figure><p>其中 sdb 就是 U 盘，而 sdb1 就是其第一个分区，其他同理；下面的 nvme0n1 只是协议不同，将其一样看成一个硬盘就行。<br>从上面打印出来的内容可以看出 sdb1 已经被挂载到“&#x2F;run&#x2F;media&#x2F;guo&#x2F;Arch”，可以使用 umount 命令将其卸载：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo umount /dev/sdb1</span><br></pre></td></tr></table></figure><div class="note warning"><p>sdb 已挂载的分区要全部卸载。</p></div></li><li><p>使用 dd 命令将镜像刻录到 U 盘中：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo dd bs=4M if=path/to/archlinux-x86_64.iso of=/dev/sdb conv=fsync oflag=direct status=progress</span><br></pre></td></tr></table></figure><div class="note warning"><p>if 参数是 iso 镜像的路径，of 参数是 U 盘的设备路径，可能会是 &#x2F;dev&#x2F;sdc 或其他路径，这里一定要指定正确的路径，错误的路径可能会破坏当前系统！</p></div></li></ol></div><div class="tab-pane" id="tab-id-2"><ol><li><p>获取管理员权限</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">sudo su - root</span><br></pre></td></tr></table></figure><p>输入开机密码</p></li><li><p>查询 iso 镜像路径</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pwd</span><br></pre></td></tr></table></figure><p>我的在“&#x2F;Users&#x2F;guo&#x2F;Desktop&#x2F;archlinux-x86_64.iso”。</p></li><li><p>查看 U 盘挂载点</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">df -h</span><br></pre></td></tr></table></figure><p>我的是“&#x2F;dev&#x2F;disk2”。</p></li><li><p>卸载 U 盘</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">diskutil unmountDisk /dev/disk2</span><br></pre></td></tr></table></figure></li><li><p>刻录镜像</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dd if=/Users/guo/Desktop/archlinux-x86_64.iso of=/dev/disk2 bs=4m</span><br></pre></td></tr></table></figure></li></ol><blockquote><p>步骤大致与 Linux 相同，只是部分命令不同而已。</p></blockquote></div><div class="tab-pane" id="tab-id-3"><p>下载软件<a href="https://sourceforge.net/projects/usbwriter/">USBWriter</a></p><p>source file 选择 iso 镜像，Target device 选择 U 盘，点击 Write 进行刻录。</p><blockquote><p>若 Windows 系统无法识别 U 盘，可能需要格式化。</p></blockquote></div></div></div><h2 id="从U盘启动Arch-live环境"><a href="#从U盘启动Arch-live环境" class="headerlink" title="从U盘启动Arch live环境"></a>从U盘启动Arch live环境</h2><p>在 BIOS 中设置启动磁盘为刚刚写入镜像的 U 盘，一般来说开机按<code>F12</code>即可选择启动盘，如果不行，则需<code>F2</code>或<code>DEL</code>进入 BIOS 修改 U 盘为优先启动，注意装完系统后将U盘启动改回后面去。<br>进入 U 盘的启动引导程序后，选择第一项即可。</p><h3 id="连接网络"><a href="#连接网络" class="headerlink" title="连接网络"></a>连接网络</h3><ul><li><p>查看连接：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ip link</span><br></pre></td></tr></table></figure></li><li><p>连接<br> 对于有线网络，安装镜像启动的时候，会默认启动 dhcpcd，如果没有启动，可以手动启动：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dhcpcd</span><br></pre></td></tr></table></figure><p> 对于无线网络，官方推荐使用<a href="https://wiki.archlinux.org/title/Iwd_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#iwctl">iwctl</a></p></li></ul><h3 id="磁盘分区"><a href="#磁盘分区" class="headerlink" title="磁盘分区"></a>磁盘分区</h3><ul><li><p>查看磁盘设备</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">lsblk -f</span><br></pre></td></tr></table></figure></li><li><p>新建分区表：<br> 由于我的本子有两块硬盘（sda、sdb），一块固态，一块机械，所以我将固态盘作为系统分区，机械盘作为数据分区，即 root、boot、swap 分区在固态盘上，home 分区在机械盘上。</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fdisk /dev/sda</span><br></pre></td></tr></table></figure><ol><li>输入<code>g</code>，新建 GPT 分区表</li><li>输入<code>w</code>，保存修改，这个操作会抹掉磁盘所有数据，慎重执行！</li><li>对<code>/dev/sdb</code>做同样处理。</li></ol></li><li><p>分区创建<br> 1扇区 &#x3D; 512字节</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fdisk /dev/sda</span><br></pre></td></tr></table></figure><ol><li>新建 EFI System 分区（非UEFI引导可以省略此步）<ol><li>输入<code>n</code></li><li>选择分区区号，直接<code>Enter</code>，使用默认值，fdisk 会自动递增分区号</li><li>分区开始扇区号，直接<code>Enter</code>，使用默认值</li><li>分区结束扇区号，输入<code>+512M</code>（推荐大小）</li><li>输入<code>t</code>修改刚刚创建的分区类型</li><li>选择分区号，直接<code>Enter</code>， 使用默认值，fdisk 会自动选择刚刚新建的分区</li><li>输入<code>1</code>，使用 EFI System 类型</li></ol></li><li>新建 Linux root (x86-64) 分区<ol><li>输入<code>n</code></li><li>选择分区区号，直接<code>Enter</code>，使用默认值，fdisk 会自动递增分区号</li><li>分区开始扇区号，直接<code>Enter</code>，使用默认值</li><li>分区结束扇区号，这里要考虑预留给 swap 分区空间，计算公式：root 分区结束扇区号 &#x3D; 磁盘结束扇区号 - 分配给 swap 分区的空间 (GB) <em>1024</em> 1024 * 1024 &#x2F; 512，输入后<code>Enter</code></li><li>输入<code>t</code>修改刚刚创建的分区类型</li><li>选择分区号，直接<code>Enter</code>， 使用默认值，fdisk 会自动选择刚刚新建的分区</li><li>输入<code>23</code>，使用 Linux root (x86-64) 类型</li></ol></li><li>新建 Linux swap 分区（使用<a href="https://wiki.archlinux.org/title/Swap_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E4%BA%A4%E6%8D%A2%E6%96%87%E4%BB%B6">交换文件</a>可以省略此步）<ol><li>输入<code>n</code></li><li>选择分区区号，直接<code>Enter</code>，使用默认值，fdisk 会自动递增分区号</li><li>分区开始扇区号，直接 Enter，使用默认值</li><li>分区结束扇区号，比如<code>+8G</code>，这里直接使用默认值，剩下的空间均给 swap 分区</li><li>输入<code>t</code>修改刚刚创建的分区类型</li><li>选择分区号，直接<code>Enter</code>， 使用默认值，fdisk 会自动选择刚刚新建的分区</li><li>输入<code>19</code>，使用 Linux swap 类型</li></ol></li><li>保存新建的分区<ol><li>输入<code>w</code></li></ol></li></ol></li><li><p>机械盘进行分区：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">fdisk /dev/sdb</span><br></pre></td></tr></table></figure><ol><li>新建 Linux root (x86-64) 分区<ol><li>输入<code>n</code></li><li>选择分区区号，直接<code>Enter</code>，使用默认值，fdisk 会自动递增分区号</li><li>分区开始扇区号，直接<code>Enter</code>，使用默认值</li><li>分区结束扇区号，直接<code>Enter</code>，使用默认值，使用全部空间</li><li>输入<code>t</code>修改刚刚创建的分区类型</li><li>选择分区号，直接<code>Enter</code>， 使用默认值，fdisk 会自动选择刚刚新建的分区</li><li>输入<code>23</code>，使用 Linux root (x86-64) 类型</li></ol></li><li>保存新建的分区<ol><li>输入<code>w</code></li></ol></li></ol></li></ul><h3 id="磁盘格式化"><a href="#磁盘格式化" class="headerlink" title="磁盘格式化"></a>磁盘格式化</h3><ul><li><p>格式化 EFI System 分区：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mkfs.fat -F32 /dev/sda1</span><br></pre></td></tr></table></figure><p> 如果格式化失败，可能是磁盘设备存在 Device Mapper，一般来说使用了 lvm，会存在这种情况</p><ul><li>显示 dm 状态：</li></ul> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dmsetup status</span><br></pre></td></tr></table></figure><ul><li>删除 dm：</li></ul> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">dmsetup remove &lt;dev-id&gt;</span><br></pre></td></tr></table></figure></li><li><p>格式化 Linux root 分区：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mkfs.ext4 /dev/sda1</span><br><span class="line">mkfs.ext4 /dev/sdb2</span><br></pre></td></tr></table></figure></li><li><p>格式化 Linux swap 分区：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mkswap /dev/sda3</span><br><span class="line">swapon /dev/sda3</span><br></pre></td></tr></table></figure></li></ul><h3 id="挂载文件系统"><a href="#挂载文件系统" class="headerlink" title="挂载文件系统"></a>挂载文件系统</h3><p>在这里需要将除 swap 分区外，其他分区都挂上去。</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">mount /dev/sda2 /mnt</span><br><span class="line">mkdir /mnt/boot</span><br><span class="line">mkdir /mnt/home</span><br><span class="line">mount /dev/sda1 /mnt/boot</span><br><span class="line">mount /dev/sdb1 /mnt/home</span><br></pre></td></tr></table></figure><blockquote><p>先后顺序不能错，要先挂载根分区，在根分区创建目录后才能挂载其他分区。</p></blockquote><p>交换分区启用:</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">mkswap /dev/sda3</span><br><span class="line">swapon /dev/sda3</span><br></pre></td></tr></table></figure><h3 id="配置-pacman-mirror"><a href="#配置-pacman-mirror" class="headerlink" title="配置 pacman mirror"></a>配置 pacman mirror</h3><p>一般来说，不需要进行修改，如果发现下载速度很慢，可以修改其中顺序或加入<a href="https://wiki.archlinux.org/title/Mirrors_(%E7%AE%80%E4%BD%93%E4%B8%AD%E6%96%87)#%E4%B8%AD%E5%9B%BD">中国节点</a>的地址</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">vim /etc/pacman.d/mirrorlist</span><br></pre></td></tr></table></figure><p>或直接使用<code>reflector</code>对源速度进行排序：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">reflector --country China --protocol http --protocol https --latest 5 --save /etc/pacman.d/mirrorlist</span><br></pre></td></tr></table></figure><h3 id="安装-Arch-和-Package-Group"><a href="#安装-Arch-和-Package-Group" class="headerlink" title="安装 Arch 和 Package Group"></a>安装 Arch 和 Package Group</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pacstrap /mnt base base-devel linux linux-firmware</span><br></pre></td></tr></table></figure><h3 id="生成-fstab-文件"><a href="#生成-fstab-文件" class="headerlink" title="生成 fstab 文件"></a>生成 fstab 文件</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">genfstab -U /mnt &gt;&gt; /mnt/etc/fstab</span><br></pre></td></tr></table></figure><h3 id="切换至安装好的-Arch"><a href="#切换至安装好的-Arch" class="headerlink" title="切换至安装好的 Arch"></a>切换至安装好的 Arch</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">arch-chroot /mnt</span><br></pre></td></tr></table></figure><h3 id="安装必备工具"><a href="#安装必备工具" class="headerlink" title="安装必备工具"></a>安装必备工具</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pacman -S vim networkmanager sudo</span><br></pre></td></tr></table></figure><h3 id="本地化"><a href="#本地化" class="headerlink" title="本地化"></a>本地化</h3><ul><li><p>修改<code>/etc/locale.gen</code>，取消注释下面这两行配置：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">en_US.UTF-8 UTF-8</span><br><span class="line">zh_CN.UTF-8 UTF-8</span><br></pre></td></tr></table></figure></li><li><p>生成 locale 信息：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">locale-gen</span><br></pre></td></tr></table></figure></li><li><p>创建<code>/etc/locale.conf</code>并写入：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">LANG=en_US.UTF-8</span><br></pre></td></tr></table></figure></li></ul><h3 id="网络配置"><a href="#网络配置" class="headerlink" title="网络配置"></a>网络配置</h3><ul><li><p>修改 hostname，创建<code>/etc/hostname</code>并写入（可替换为其他名称）：</p> <figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Arch</span><br></pre></td></tr></table></figure></li><li><p>配置 hosts，编辑<code>/etc/hosts</code></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">127.0.0.1 localhost</span><br><span class="line">::1    localhost</span><br><span class="line">127.0.1.1 Arch.localdomain</span><br></pre></td></tr></table></figure></li></ul><h3 id="修改-root-密码"><a href="#修改-root-密码" class="headerlink" title="修改 root 密码"></a>修改 root 密码</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">passwd</span><br></pre></td></tr></table></figure><h3 id="安装-Microcode"><a href="#安装-Microcode" class="headerlink" title="安装 Microcode"></a>安装 Microcode</h3><ul><li>AMD CPU：</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pacman -S amd-ucode</span><br></pre></td></tr></table></figure><ul><li>Intel CPU：</li></ul><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">pacman -S intel-ucode</span><br></pre></td></tr></table></figure><h3 id="安装-GRUB"><a href="#安装-GRUB" class="headerlink" title="安装 GRUB"></a>安装 GRUB</h3><p>MBR 引导：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pacman -S grub</span><br><span class="line">grub-install --target=i386-pc /dev/sda</span><br><span class="line">grub-mkconfig -o /boot/grub/grub.cfg</span><br></pre></td></tr></table></figure><p>UEFI 引导：</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pacman -S grub efibootmgr</span><br><span class="line">grub-install --target=x86_64-efi --efi-directory=/boot --bootloader-id=grub</span><br><span class="line">grub-mkconfig -o /boot/grub/grub.cfg</span><br></pre></td></tr></table></figure><h2 id="重新启动"><a href="#重新启动" class="headerlink" title="重新启动"></a>重新启动</h2><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">exit    # 退出 chroot 环境，或按 Ctrl+D</span><br><span class="line">reboot</span><br></pre></td></tr></table></figure><h3 id="重启后的设置"><a href="#重启后的设置" class="headerlink" title="重启后的设置"></a>重启后的设置</h3><p>使用root账户登录后：</p><ul><li><p>开启时间自动同步</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">timedatectl set-ntp true</span><br><span class="line">timedatectl set-timezone Asia/Shanghai</span><br></pre></td></tr></table></figure></li><li><p>新建用户</p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">useradd -m -g wheel &lt;username&gt;</span><br></pre></td></tr></table></figure></li></ul>]]></content>
      
      
      <categories>
          
          <category> ArchLinux </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Linux </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
